<Window x:Name="winnMain" x:Class="PacChat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PacChat"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Pacchat" Height="499.5" Width="836.5" UseLayoutRounding="False" WindowStartupLocation="CenterScreen" Background="{x:Null}" WindowStyle="None" Padding="0">
    <WindowChrome.WindowChrome>
        <WindowChrome 
        CaptionHeight="0"
        ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Storyboard x:Key="left-side-panel-expand">
            <DoubleAnimation
                Storyboard.TargetName="LeftSidePanel"
                Storyboard.TargetProperty="Width"
                From="32" To="128" Duration="0:0:0.25"
                AutoReverse="False">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

        </Storyboard>
        <Storyboard x:Key="left-side-panel-compress">
            <DoubleAnimation
                Storyboard.TargetName="LeftSidePanel"
                Storyboard.TargetProperty="Width"
                From="128" To="32" Duration="0:0:0.25"
                AutoReverse="False">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <DockPanel>
        <!--stackPanel HEADER-->
        <StackPanel x:Name="spanelHeader" DockPanel.Dock="Top" Height="32" Background="#FF4F4F4F" MouseMove="FormDrag">
            <DockPanel Height="32" HorizontalAlignment="Right" Width="96">
                <Button x:Name="BtnQuit"
                Style="{DynamicResource MaterialDesignRaisedButton}" HorizontalAlignment="Right"
                materialDesign:ShadowAssist.ShadowDepth="Depth0" Width="32" Padding="0" Background="#FF4F4F4F" BorderBrush="{x:Null}" Margin="0" DockPanel.Dock="Right" Height="32" VerticalAlignment="Bottom" Click="BtnQuit_Click" Cursor="Arrow">
                    <materialDesign:PackIcon
                    Kind="Close"
                    Height="16"
                    Width="16" Foreground="#DDFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>

                <Button x:Name="BtnMaximize"
                Style="{DynamicResource MaterialDesignRaisedButton}" HorizontalAlignment="Center"
                materialDesign:ShadowAssist.ShadowDepth="Depth0" Width="32" Padding="0" Background="#FF4F4F4F" BorderBrush="{x:Null}" DockPanel.Dock="Right" Height="32" VerticalAlignment="Bottom" Click="BtnMaximize_Click" Cursor="Arrow">
                    <materialDesign:PackIcon
                    Kind="WindowMaximize"
                    Height="16"
                    Width="16" Foreground="#DDFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>

                <Button x:Name="BtnMinimize"
                Style="{DynamicResource MaterialDesignRaisedButton}" HorizontalAlignment="Center"
                materialDesign:ShadowAssist.ShadowDepth="Depth0" Width="32" Padding="0" Background="#FF4F4F4F" BorderBrush="#FF4F4F4F" DockPanel.Dock="Right" Height="32" VerticalAlignment="Top" Click="BtnMinimize_Click" Cursor="Arrow">
                    <materialDesign:PackIcon
                    Kind="WindowMinimize"
                    Height="16"
                    Width="16" Foreground="#DDFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>
            </DockPanel>




        </StackPanel>

        <!--stackPanel SIDEMENU-->
        <StackPanel x:Name="LeftSidePanel" Background="#FF878787" HorizontalAlignment="Left" Width="32">
            <Button Background="{x:Null}" BorderBrush="{x:Null}" Height="48" Width="Auto" Foreground="White" Padding="0" Cursor="Arrow" >
                <materialDesign:PackIcon Kind="MoreHoriz" Width="24" Height="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="#DDFFFFFF" />
            </Button>

            <Button x:Name="BtnNoti" Background="{x:Null}" BorderBrush="{x:Null}" Height="48" Foreground="White" Padding="4,0,0,0" Click="BtnNoti_Click" Cursor="Arrow" >
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Notifications" Width="24" Height="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="#DDFFFFFF" />
                    <Label Content="Notifications" Foreground="#FFF1F1F1"></Label>
                </StackPanel>
            </Button>

            <Button x:Name="Btn2" Background="{x:Null}" BorderBrush="{x:Null}" Height="48" Foreground="White" Padding="4,0,0,0" Click="Btn2_Click" Cursor="Arrow" >
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="User" Width="24" Height="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="#DDFFFFFF" />
                    <Label Content="Online users" Foreground="#FFF1F1F1"></Label>
                </StackPanel>
            </Button>

            <Button x:Name="Btn3" Background="{x:Null}" BorderBrush="{x:Null}" Height="48" Width="Auto" Foreground="White" Padding="4,0,0,0" HorizontalContentAlignment="Left" Click="Btn3_Click" Cursor="Arrow" >
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="UserBoxes" Width="24" Height="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="#DDFFFFFF" />
                    <Label Content="Contact list" Foreground="#FFF1F1F1"></Label>
                </StackPanel>
            </Button>

        </StackPanel>
        <StackPanel x:Name="spanelContent" Background="#FFF1F1F1">
            <materialDesign:DrawerHost
  Margin="0"
  BorderThickness="2"
  BorderBrush="{DynamicResource MaterialDesignDivider}" Padding="0,0,0,1" Width="482" Height="400" HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:DrawerHost.LeftDrawerContent>
                    <StackPanel
                        Margin="16,0,16,30">
                        <TextBlock
                            Margin="4"
                            HorizontalAlignment="Center"><Run Text="LEFT FIELD"/></TextBlock>
                        <Button
                            Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                            CommandParameter="{x:Static Dock.Left}"
                            Margin="4"
                            HorizontalAlignment="Center"
                            Style="{DynamicResource MaterialDesignFlatButton}" Content="CLOSE THIS"/>
                        <Button                              Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"                              Margin="4"                              HorizontalAlignment="Center"                              Style="{DynamicResource MaterialDesignFlatButton}" Content="CLOSE ALL"/>
                    </StackPanel>
                </materialDesign:DrawerHost.LeftDrawerContent>
                <materialDesign:DrawerHost.TopDrawerContent>
                    <StackPanel
      Margin="16"
      HorizontalAlignment="Center"
      Orientation="Horizontal" Width="308">
                        <TextBlock
        Margin="4"
        VerticalAlignment="Center"><Run Text="TOP BANANA"/></TextBlock>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        Style="{DynamicResource MaterialDesignFlatButton}"
        Margin="4"
        VerticalAlignment="Center" Content="CLOSE ALL"/>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        CommandParameter="{x:Static Dock.Top}"
        Style="{DynamicResource MaterialDesignFlatButton}"
        Margin="4"
        VerticalAlignment="Center" Content="CLOSE THIS"/>
                    </StackPanel>
                </materialDesign:DrawerHost.TopDrawerContent>
                <materialDesign:DrawerHost.RightDrawerContent>
                    <StackPanel
      Margin="16">
                        <TextBlock
        Margin="4"
        HorizontalAlignment="Center"><Run Text="THE RIGHT STUFF"/></TextBlock>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        CommandParameter="{x:Static Dock.Right}"
        Margin="4"
        HorizontalAlignment="Center"
        Style="{DynamicResource MaterialDesignFlatButton}" Content="CLOSE THIS"/>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        Margin="4"
        HorizontalAlignment="Center"
        Style="{DynamicResource MaterialDesignFlatButton}" Content="CLOSE ALL"/>
                    </StackPanel>
                </materialDesign:DrawerHost.RightDrawerContent>
                <materialDesign:DrawerHost.BottomDrawerContent>
                    <StackPanel
      Margin="16"
      HorizontalAlignment="Center"
      Orientation="Horizontal">
                        <TextBlock
        Margin="4"
        VerticalAlignment="Center"><Run Text="BOTTOM BRACKET"/></TextBlock>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        Style="{DynamicResource MaterialDesignFlatButton}"
        Margin="4"
        VerticalAlignment="Center" Content="CLOSE ALL"/>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
        CommandParameter="{x:Static Dock.Bottom}"
        Style="{DynamicResource MaterialDesignFlatButton}"
        Margin="4"
        VerticalAlignment="Center" Content="CLOSE THIS"/>
                    </StackPanel>
                </materialDesign:DrawerHost.BottomDrawerContent>
                <Grid
    MinWidth="480"
    MinHeight="480">
                    <Grid
      VerticalAlignment="Center"
      HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        CommandParameter="{x:Static Dock.Left}"
        Grid.Row="1"
        Grid.Column="0"
        Margin="4">
                            <materialDesign:PackIcon
          Kind="ArrowLeft" />
                        </Button>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        CommandParameter="{x:Static Dock.Top}"
        Grid.Row="0"
        Grid.Column="1"
        Margin="4">
                            <materialDesign:PackIcon
          Kind="ArrowUp" />
                        </Button>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        CommandParameter="{x:Static Dock.Right}"
        Grid.Row="1"
        Grid.Column="2"
        Margin="4">
                            <materialDesign:PackIcon
          Kind="ArrowRight" />
                        </Button>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        CommandParameter="{x:Static Dock.Bottom}"
        Grid.Row="2"
        Grid.Column="1"
        Margin="4">
                            <materialDesign:PackIcon
          Kind="ArrowDown" />
                        </Button>
                        <Button
        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        Grid.Row="1"
        Grid.Column="1"
        Margin="4"
        Style="{DynamicResource MaterialDesignRaisedAccentButton}">
                            <materialDesign:PackIcon
          Kind="ArrowAll" />
                        </Button>
                    </Grid>
                </Grid>
            </materialDesign:DrawerHost>
        </StackPanel>

    </DockPanel>

</Window>
